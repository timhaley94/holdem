// const { onStart, onConnect } = require('./index');

describe('API.Socket.Game', () => {
  describe('.onStart()', () => {
    it('placeholder', () => {

    });
  });

  describe('.onConnect()', () => {
    it('placeholder', () => {

    });
  });
});
