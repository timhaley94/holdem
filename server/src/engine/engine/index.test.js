const Game = require('../game');
const Engine = require('./index');

describe('Engine.engine', () => {
  describe('.init()', () => {
    
  });
  
  describe('.Game', () => {
    expect(Engine.Game).toBe(Game);
  });
});
