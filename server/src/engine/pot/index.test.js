describe('Engine.pot', () => {
  describe('.create()', () => {
    
  });
  
  describe('.bet()', () => {
    it('throws error on invalid user id', () => {

    });

    it('throws error on negative number', () => {

    });
  });
  
  describe('.fold()', () => {
    
  });

  describe('.resolve()', () => {
    
  });
});
